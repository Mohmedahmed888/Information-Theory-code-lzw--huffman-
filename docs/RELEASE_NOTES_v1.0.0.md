# Release `v1.0.0` — Information Theory Compression Toolkit

Coursework-oriented bundle: Huffman + LZW codecs, Tkinter GUI, Flask web UI, entropy utilities, optional Hamming/BSC demo, and `test_all.py` for scripted checks.

## Verify locally

```bash
pip install -r requirements.txt
python test_all.py
```
