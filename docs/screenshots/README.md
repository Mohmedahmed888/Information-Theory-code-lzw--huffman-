# Screenshots (optional assets for README / submissions)

Place static images here to keep the repository root tidy.

Suggested captures (PNG or JPG):

1. **`web-home.png`** — Full browser window showing the Flask UI after a successful compression run  
2. **`gui-home.png`** — Tkinter GUI (`gui/app.py`) with file selected and results visible  
3. **`comparison-tab.png`** (optional) — Web UI “Comparison” tab with Huffman vs LZW side-by-side

Then reference them from the repository root README, for example:

```markdown
<img src="docs/screenshots/web-home.png" alt="Web UI" width="780"/>
```

> GitHub renders images from paths relative to the repository root.
